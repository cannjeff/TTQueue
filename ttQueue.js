/*
 *	TTQueue, Queue management for turntable.fm
 *
 *	Written by: Jeff Cann (@cannjeff)
 */

(function(window) {
	window.ttQueue = {
		init: function() {
			console.log('run');
		}
	}
})(window);

ttQueue.init();